{
  "name": "draftjs_exporter",
  "version": "0.0.0-dev",
  "private": true,
  "description": "Library to convert rich text from Draft.js raw ContentState to HTML",
  "license": "MIT",
  "devDependencies": {
    "prettier": "3.9.6"
  },
  "scripts": {
    "lint": "prettier --check '**/?(.)*.{md,css,scss,js,json,jsonc,yaml,yml,html}'",
    "format": "prettier --write '**/?(.)*.{md,css,scss,js,json,jsonc,yaml,yml,html}'"
  }
}
